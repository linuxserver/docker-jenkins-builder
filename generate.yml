---

  - hosts: localhost
    vars_files:
      - vars/common
    pre_tasks:
      - set_fact:
          container_name: "{{ lookup('env', 'CONTAINER_NAME' ) }}"
    roles:
        - { role: roles/generate-docs }