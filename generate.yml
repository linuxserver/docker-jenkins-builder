---

  - hosts: localhost
    vars_files:
      - vars/common
    pre_tasks:
      - set_fact:
          container_name: "{{ lookup('env', 'NAME' ) | ternary (lookup('env', 'NAME' ), omit) }}"
    roles:
        - { role: roles/generate-docs }